1.为什么要用monorepo的微前端。
    之前的老项目是一个SPA结构，技术栈也比较老旧，我们希望渐进式升级。同时也考虑的项目需要多个团队维护。

2.如何观察性能的？使用iframe你觉得性能差的原因是什么。
    常见指标FP，FCP，LCP。FMP需要自己计算，通过MutationObservers观察变化的DOM，并根据权重打分，一段时间后分数变化变缓慢，则上报首屏时间。
    iframe有独立的内存成本、有渲染成本无法与主应用同步绘制、postMessage成本、冷启动慢。

3.数据同步是如何做的？
    挂载在windows下。

4.前端低代码平台是做了什么工作，主要服务哪些用户。

5.如何实现生码结果的稳定的。

6.宏任务和微任务的题。
```js

// 直接的两个then都会进入同一个微任务队列
new Promise().reslove().then().then();

// 可以当作持续执行的setTimeOut
setInterver()

```

7.react代码。实现一个深度比较的UseEffect。

8.一个正整数的加减乘除计算器。